<template>
  <div>
    这是404界面
    <!-- <template v-for="link in links">
      <template v-if="link.subs">
        <el-submenu :index="link.id" :key="link.id">
          <template #title><span>{{ link.title }}</span></template>
          <el-menu-item v-for="sub in link.subs" :index="sub.id">
            <span>{{ sub.title }}</span>
          </el-menu-item>
        </el-submenu>
      </template>
      <template v-else>
        <el-menu-item :index="link.id" :key="link.id">
          <template #title><span>{{ link.title }}</span></template>
        </el-menu-item>
      </template>
    </template> -->
  </div>
</template>

<script setup>

</script>

<style lang="scss" scoped></style>